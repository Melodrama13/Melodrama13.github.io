# 活动页视野稳定修复

## Goal
统一活动页显隐与定位时的滚动锚点，保证移动端视野中的活动不漂移，并消除“当期”二段跳。

## Tasks
- [x] 追踪三个显隐按钮、渐进渲染和“当期”定位的滚动调用链 → Verify: 找到单帧锚点恢复与延迟强制纠正的冲突。
- [x] 将显隐操作改为可取消的多帧中心活动锚点事务 → Verify: 显隐期间持续锁定活动 ID 与相对位置，手动滚动会立即取消校正。
- [x] 简化“当期”定位，避免估算位置上的平滑滚动 → Verify: 当期改用预热完成后的即时定位，不再启动 900/1400ms 的平滑滚动纠正。
- [x] 回归保存位置、顶部/底部和活动定位 → Verify: Chromium/WebKit 均能到达 0px 顶部与 0px 底部差值，活动 212 高亮保持；新跳转会取消旧锚点事务。
- [x] 运行移动端 WebKit/Chromium 检查、构建与差异检查 → Verify: 两个引擎中三个显隐按钮的目标活动漂移均不超过 1.6px；构建与差异检查通过。

## Done When
- [x] 三个显隐按钮在手机布局下保持中心活动不变，“当期”只产生一次目标滚动；桌面布局沿用同一锚点路径。

## Notes
保留用户已有的 `public/data/pjsk_songs.json` 修改，不启动常驻开发服务器。
